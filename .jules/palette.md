## 2024-08-08 - Accessible Icon Buttons
**Learning:** Icon-only buttons lack context for screen readers and some keyboard users. Providing an `aria-label` provides the necessary context.
**Action:** Adding `aria-label` to icon-only toggle and action buttons across the interface to improve accessibility and provide helpful tooltips.
