## 2026-08-27 - Added Turkish ARIA Labels to Dashboard
**Learning:** Icon-only buttons lack context for screen reader users and impact accessibility. I noticed that ensuring labels are correctly translated to Turkish (like 'Bildirimler' and 'Temayı Değiştir') is critical to maintaining a consistent language experience across the platform's UI components.
**Action:** Use Turkish translations consistently when writing `aria-label` attributes for all interactive components.
