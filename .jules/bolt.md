## 2024-05-18 - KanbanBoard Render Loop Performance Pattern\n**Learning:** Subtask array mapping (`getSubtaskCount`) and column group sorting (`getColumnTasks`) were recalculating every render loop for every column and task individually, yielding exponentially compounding O(N^2) render passes as board load increases.\n**Action:** Always hoist aggregate structural filtering into `useMemo` map bindings ahead of map iterators in dense functional components.
